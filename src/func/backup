#!/usr/bin/env bash
# tar + gpg backups

backup_Wallets()
{
	if [[ -f "$dotMoneroBash/wallets.tar" ]]; then
		print_Error_Exit "$dotMoneroBash/wallets.tar already exists!"
	elif [[ -f "$dotMoneroBash/wallets.tar.gpg" ]]; then
		print_Error_Exit "$dotMoneroBash/wallets.tar.gpg already exists!"
	fi
	$bblue; echo "Backing up wallets..."
	$bwhite; echo -n "Cipher algo: " ;$white; echo "AES256"
	$bwhite; echo -n "Key type: " ;$white; echo "Passphrase"
	tar -cz "$wallets" "$dotMoneroBash/backup" &>/dev/null | gpg -c --no-symkey-cache --cipher-algo AES256 -o "$dotMoneroBash/backup.tar.gpg" &>/dev/null
	error_Exit "GPG error"
	$bwhite; echo -n "Encrypted wallet tar: " ;$white; echo "$dotMoneroBash/backup.tar.gpg"
	$bwhite; echo -n "To decrypt: " ;$bred; echo -n "monero-bash decrypt"
	$bwhite; echo -n " OR " ;$bred; echo "gpg -d backup.tar.gpg | tar -xzf -"
}

backup_Decrypt()
{
	[[ -d "$dotMoneroBash/backup" ]]&& print_Error_Exit "$dotMoneroBash/backup already exists, please move!"
	[[ ! -f "$dotMoneroBash/backup.tar.gpg" ]]&& print_Error_Exit "$dotMoneroBash/backup.tar.gpg was not found!"
	$bblue; echo "Decrypting wallets..."
	cd "$dotMoneroBash"
	gpg -d backup.tar.gpg | tar -xzf -
	error_Exit "Decrypt failed"
	$bwhite; echo -n "Decrypted wallets: " ;$white; echo "$dotMoneroBash/backup"
}
