# [monero-bash systemd services]

The [systemd] files created by [monero-bash] are created dynamically at that moment. These files are just the reference for what a typical service should look like.

